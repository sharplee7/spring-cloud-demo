server:
  port: 8082
  servlet:
    context-path: /mock-service
spring:
  application:
    name: mock-service
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: 'true'

